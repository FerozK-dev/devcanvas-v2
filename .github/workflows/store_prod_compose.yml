name: Upload Compose File to GHCR

on:
  push:
    paths:
      - 'docker-compose.prod.yml'
  workflow_dispatch:
    inputs:
      ref:
        description: 'Branch or tag to use'
        required: true
        default: 'master'

permissions:
  contents: read
  packages: write

jobs:
  upload-compose:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.ref || github.ref }} # Use input ref or current ref

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT }} # Change from GHCR_PAT to GITHUB_TOKEN

      - name: Upload compose file as config
        run: |
          # Verify file exists
          ls -la docker-compose.prod.yml || exit 1

          # Create temporary Dockerfile
          cat <<EOF > Dockerfile.temp
          FROM scratch
          COPY docker-compose.prod.yml /docker-compose.prod.yml
          EOF

          # Build and push config image
          docker buildx build \
            -f Dockerfile.temp \
            -t ghcr.io/${{ github.repository_owner }}/devcanvas-config:latest \
            -t ghcr.io/${{ github.repository_owner }}/devcanvas-config:${{ github.sha }} \
            --push \
            --platform linux/amd64 .